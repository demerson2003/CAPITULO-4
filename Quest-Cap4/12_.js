// Questão 12

/* 12. Escreva um programa que peça ao usuário para digitar sua altura em metros e converta-a para número de ponto flutuante usando `parseFloat()`.
    Em seguida, exiba a altura convertida no console.

*/

    const altura = prompt('Digite sua altura em metros (ex: 1.75):');

    const alturaConvertida = parseFloat(altura);

    console.log('Sua altura convertida é:', alturaConvertida);


